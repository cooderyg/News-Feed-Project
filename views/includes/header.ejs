<header>
  <div class="wrap">
    <h1><a href="/"></a></h1>
    <div style="display: flex;">
      <% if(login==1) { %>
        <span style="margin-right: 10px;">
          <%= name %>
        </span>
        <a class="login" href="/api/users/signout">로그아웃</a>
        <% } else { %>
          <a class="login" href="/login">로그인</a>
          <% } %>
    </div>
  </div>
</header>